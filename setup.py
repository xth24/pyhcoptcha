from setuptools import setup

setup(
    name="hcoptcha",
    version="0.1",
    description="A Wrapper for hcoptcha captchs solving service",
    packages=["hcoptcha"],
    install_requires=["requests"],
)
